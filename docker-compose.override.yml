# Override file: contains secrets / sensitive overrides.
# For production consider NOT committing this file; instead supply
# variables via your deployment system (Docker secrets, Swarm, K8s, Vault).

services:
  admin-module:
    environment:
      - ADMIN_POSTGRES_PASSWORD=supersecretpassword
      - ADMIN_SECRET_KEY=CHANGE_ME_SUPER_SECRET
